---
phase: 04-ai-chat-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/app/components/chat/ChatPanel.tsx
  - frontend/app/page.tsx
  - test/specs/chat.spec.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06

must_haves:
  truths:
    - "User can type a message and send it to the AI with Enter key or Send button"
    - "A loading spinner labelled 'FinAlly is thinking...' is visible between submission and response arrival"
    - "Assistant response text appears in the chat conversation history"
    - "Trade confirmation chips (BUY/SELL qty ticker @ $price) appear inline below assistant message when AI executes trades"
    - "Watchlist chips (+ TICKER watchlist or − TICKER watchlist) appear inline when AI changes watchlist"
    - "Chat panel collapses to 40px width and re-expands to 320px via a toggle button; message history persists"
    - "After AI response, positions table and cash balance update without page reload"
  artifacts:
    - path: "frontend/app/components/chat/ChatPanel.tsx"
      provides: "Self-contained chat UI: input, messages list, spinner, chips, scroll-to-bottom"
      exports: ["default ChatPanel"]
    - path: "frontend/app/page.tsx"
      provides: "Wires ChatPanel into grid area 'chat', adds collapse toggle button in header"
      contains: "ChatPanel"
    - path: "test/specs/chat.spec.ts"
      provides: "E2E tests for CHAT-01 through CHAT-06 against LLM_MOCK=true container"
      contains: "chat panel"
  key_links:
    - from: "frontend/app/components/chat/ChatPanel.tsx"
      to: "frontend/app/providers/PortfolioContext.tsx"
      via: "usePortfolio().refetchPortfolio()"
      pattern: "refetchPortfolio"
    - from: "frontend/app/components/chat/ChatPanel.tsx"
      to: "frontend/app/providers/WatchlistContext.tsx"
      via: "useWatchlist().refetchWatchlist()"
      pattern: "refetchWatchlist"
    - from: "frontend/app/components/chat/ChatPanel.tsx"
      to: "/api/chat"
      via: "fetch POST /api/chat"
      pattern: "fetch.*api/chat"
    - from: "frontend/app/page.tsx"
      to: "frontend/app/components/chat/ChatPanel.tsx"
      via: "import and render in aside gridArea='chat'"
      pattern: "ChatPanel"
---

## Phase Goal

**As a** trader, **I want to** talk to FinAlly's AI assistant, **so that** I can get portfolio analysis and have trades executed on my behalf through natural language.

<objective>
Build the complete AI chat panel as a vertical slice: the ChatPanel component, its wiring into page.tsx including the collapse toggle, and the full Playwright E2E test suite for CHAT-01 through CHAT-06.

After this plan executes, a user can open the app, type a message to the AI, watch the spinner, read the response, see trade and watchlist chips inline, and collapse the panel to reclaim screen space.

Purpose: Delivers the core AI interaction capability that is the centerpiece of FinAlly's value proposition.
Output: One new file (ChatPanel.tsx, chat.spec.ts) and one modified file (page.tsx). All CHAT requirements satisfied.
</objective>

<execution_context>
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/richardleclezio/projects/finally/.planning/PROJECT.md
@/Users/richardleclezio/projects/finally/.planning/ROADMAP.md
@/Users/richardleclezio/projects/finally/.planning/STATE.md
@/Users/richardleclezio/projects/finally/.planning/phases/04-ai-chat-panel/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact interfaces the executor needs. Extracted from codebase and research. -->

From frontend/app/providers/PortfolioContext.tsx (after Plan 01):
```typescript
export type PortfolioContextValue = {
  positions: Position[]
  cashBalance: number
  totalValue: number
  trades: Trade[]
  history: Snapshot[]
  executeTrade: (ticker: string, quantity: number, side: 'buy' | 'sell') => Promise<string | null>
  refetchPortfolio: () => Promise<void>   // ADDED in Plan 01
}
// Access via: const { refetchPortfolio } = usePortfolio()
```

From frontend/app/providers/WatchlistContext.tsx (after Plan 01):
```typescript
export type WatchlistContextValue = {
  tickers: string[]
  selectedTicker: string | null
  setSelectedTicker: (ticker: string) => void
  priceHistory: Record<string, number[]>
  firstPrice: Record<string, number>
  addTicker: (ticker: string) => Promise<void>
  removeTicker: (ticker: string) => Promise<void>
  refetchWatchlist: () => Promise<void>  // ADDED in Plan 01
}
// Access via: const { refetchWatchlist } = useWatchlist()
```

POST /api/chat response shape (from backend/app/api/chat.py):
```typescript
{
  message: string,                          // LLM conversational text — render as assistant text
  trades: [{ticker, side, quantity}],       // LLM intent (may include failed attempts — DO NOT render as chips)
  watchlist_changes: [{ticker, action}],    // LLM intent
  executed_trades: [{                       // What actually executed — USE THESE for trade chips
    id: string,
    ticker: string,
    side: string,
    quantity: number,
    price: number,
    executed_at: string,
  }],
  watchlist_results: [{ticker, action}],    // Result: 'added'|'removed'|'not_found'|'already_in_watchlist' — USE THESE for watchlist chips
  errors: string[],                         // Trade validation errors
}
```

ChatMessage type to define locally in ChatPanel.tsx:
```typescript
type ActionChip =
  | { kind: 'trade'; ticker: string; side: string; quantity: number; price: number }
  | { kind: 'watchlist'; ticker: string; action: string }
  | { kind: 'error'; text: string }

type ChatMessage = {
  id: string
  role: 'user' | 'assistant'
  text: string
  chips?: ActionChip[]
}
```

From frontend/app/page.tsx (current state — the aside block to replace):
```typescript
{/* AI chat placeholder */}
<aside
  style={{
    gridArea: 'chat',
    backgroundColor: '#1a1a2e',
    borderLeft: '1px solid #30363d',
    overflow: 'hidden',
  }}
>
  <PlaceholderPanel label="AI CHAT" phase="Phase 4" />
</aside>
```

Color tokens (inline style values — project convention, never Tailwind classes):
- '#0d1117'  bg-base (input background)
- '#1a1a2e'  bg-panel (chat panel background)
- '#161b22'  bg-surface (message bubble background)
- '#ecad0a'  accent-yellow (panel label)
- '#209dd7'  accent-blue (watchlist chip border, send button outline)
- '#753991'  accent-purple (send button background)
- '#e6edf3'  text-primary
- '#8b949e'  text-muted (labels, panel headers)
- '#30363d'  border
- '#21262d'  border-subtle
- '#22c55e'  green (buy chip border/text)
- '#ef4444'  red (sell chip border/text, error)

Pattern from TradeBar.tsx for disabled input during loading:
```typescript
disabled={loading}
style={{ cursor: loading ? 'not-allowed' : 'pointer', opacity: loading ? 0.6 : 1 }}
```

Pattern from TradeHistory.tsx for scrollable list container:
```typescript
<div style={{ flex: 1, overflowY: 'auto' }}>
  {items.map(...)}
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatPanel.tsx component</name>
  <read_first>
    frontend/app/providers/PortfolioContext.tsx
    frontend/app/providers/WatchlistContext.tsx
    frontend/app/components/portfolio/TradeBar.tsx
    frontend/app/components/portfolio/TradeHistory.tsx
    frontend/app/components/portfolio/PortfolioPanel.tsx
    frontend/AGENTS.md
  </read_first>
  <files>frontend/app/components/chat/ChatPanel.tsx</files>
  <action>
    Create frontend/app/components/chat/ChatPanel.tsx (new file, new directory). The component is a self-contained 'use client' component. No sub-components. All styling uses inline style={{}} objects — never Tailwind utility classes (project convention confirmed from existing components).

    Structure (top to bottom):

    1. Directive and imports:
       - 'use client'
       - useState, useRef, useEffect, useCallback from 'react'
       - usePortfolio from '../providers/PortfolioContext' (note: path relative to chat/ directory — use '../../providers/PortfolioContext')
       - useWatchlist from '../../providers/WatchlistContext'

    2. Local type definitions: ActionChip (union type with kind discriminant) and ChatMessage (id, role, text, chips?) — see interfaces block above for exact shapes.

    3. Default export function ChatPanel():
       - State: messages: ChatMessage[] (init []), input: string (init ''), isLoading: boolean (init false)
       - Refs: bottomRef = useRef&lt;HTMLDivElement&gt;(null)
       - Context: const { refetchPortfolio } = usePortfolio(); const { refetchWatchlist } = useWatchlist()
       - useEffect: on messages change, call bottomRef.current?.scrollIntoView({ behavior: 'smooth' })

    4. handleSubmit = useCallback(async () => { ... }, [input, isLoading, refetchPortfolio, refetchWatchlist]):
       - Guard: if (!input.trim() || isLoading) return
       - Append user message to messages with role:'user'
       - Clear input, set isLoading true
       - try: fetch('/api/chat', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ message: text }) })
       - Parse response.json() as the API response shape
       - Build chips array from data.executed_trades (kind:'trade') and data.watchlist_results (kind:'watchlist') and data.errors (kind:'error')
       - Append assistant message with text: data.message and chips (if chips.length > 0)
       - await refetchPortfolio()
       - await refetchWatchlist()
       - catch: append assistant message with text: 'Connection error. Please try again.'
       - finally: setIsLoading(false)

    5. JSX structure (flex column, height: '100%'):
       - Header bar (height:32, flexShrink:0, borderBottom: '1px solid #30363d'): label "AI CHAT" in text-muted style (fontSize:11, fontWeight:600, letterSpacing:'0.15em')
       - Messages area (flex:1, overflowY:'auto', padding:'8px 0'):
         - Empty state when messages.length === 0: centered div with text 'Ask FinAlly anything about your portfolio' in color '#30363d', fontSize:11
         - Mapped messages: user messages right-aligned (backgroundColor:'#161b22', marginLeft:'auto'), assistant messages left-aligned (no background shift). Each message: role label in text-muted, text in text-primary. Below assistant text: chip list (flex row wrap, gap:4, padding-top:4)
         - Trade chip: display:'inline-flex', border: '1px solid' + (side==='buy' ? '#22c55e' : '#ef4444'), backgroundColor: buy ? 'rgba(34,197,94,0.15)' : 'rgba(239,68,68,0.15)', color: buy ? '#22c55e' : '#ef4444', fontSize:11, padding:'2px 8px'. Text: "{side.toUpperCase()} {quantity} {ticker} @ ${price.toFixed(2)}"
         - Watchlist chip: border:'1px solid #209dd7', backgroundColor:'rgba(32,157,215,0.15)', color:'#209dd7', fontSize:11, padding:'2px 8px'. Text: "{action==='added' ? '+' : '−'} {ticker} watchlist"
         - Error chip: border:'1px solid #ef4444', color:'#ef4444', fontSize:11, padding:'2px 8px'
       - Spinner block (shown when isLoading): flex row, alignItems:'center', gap:8, padding:'8px 12px', color:'#8b949e', fontSize:11. Contains a 12x12 div with border:'2px solid #30363d', borderTop:'2px solid #209dd7', borderRadius:'50%', animation:'spin 0.8s linear infinite'
       - Scroll anchor: &lt;div ref={bottomRef} /&gt;
       - Input area (flexShrink:0, borderTop:'1px solid #30363d', padding:8, display:'flex', gap:8):
         - &lt;input type="text"&gt;: value={input}, onChange updates input state, onKeyDown: if e.key==='Enter' && !e.shiftKey call handleSubmit and e.preventDefault(), disabled={isLoading}, placeholder="Ask FinAlly...", style matches TradeBar.tsx input style (bg #0d1117, border #30363d, color #e6edf3, height:28, fontSize:13, paddingLeft:8, borderRadius:0, outline:'none', flex:1)
         - Send button: onClick={handleSubmit}, disabled={isLoading}, text "Send", style: backgroundColor:'#753991', color:'#e6edf3', border:'none', height:28, padding:'0 12px', fontSize:12, cursor: isLoading ? 'not-allowed' : 'pointer', opacity: isLoading ? 0.6 : 1

    Critical constraints:
    - Never use dangerouslySetInnerHTML for message text — always use {message.text} (React escapes XSS)
    - Never use Server Actions or 'use server' — static export incompatible
    - Input is &lt;input type="text"&gt; not textarea (avoids Enter/newline complexity per research pitfall 4)
    - Chips are from data.executed_trades NOT data.trades (per research pitfall 1)
    - refetchWatchlist() is called, not addTicker() (avoids double-POST 409 per research pitfall 2)
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at frontend/app/components/chat/ChatPanel.tsx
    - `grep "'use client'" frontend/app/components/chat/ChatPanel.tsx` returns 1 match on line 1
    - `grep "dangerouslySetInnerHTML" frontend/app/components/chat/ChatPanel.tsx` returns 0 matches
    - `grep "executed_trades" frontend/app/components/chat/ChatPanel.tsx` returns at least 1 match (chips from executed_trades, not trades)
    - `grep "refetchPortfolio" frontend/app/components/chat/ChatPanel.tsx` returns at least 1 match
    - `grep "refetchWatchlist" frontend/app/components/chat/ChatPanel.tsx` returns at least 1 match
    - `grep "spin 0.8s" frontend/app/components/chat/ChatPanel.tsx` returns 1 match (spinner animation)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ChatPanel.tsx exists with 'use client', implements all CHAT-01 through CHAT-06 behaviors, TypeScript compiles without errors, uses executed_trades for chips, calls refetchPortfolio and refetchWatchlist after response.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ChatPanel into page.tsx + add collapse toggle</name>
  <read_first>
    frontend/app/page.tsx
    frontend/app/components/chat/ChatPanel.tsx
  </read_first>
  <files>frontend/app/page.tsx</files>
  <action>
    Four edits to frontend/app/page.tsx:

    1. Add useState to the existing import (currently no useState import). The file already has `'use client'` and imports from React providers.
       Add: `import { useState } from 'react'`

    2. Add ChatPanel import after the existing PortfolioPanel import:
       `import ChatPanel from './components/chat/ChatPanel'`

    3. Inside the Home() function body, add collapse state after the existing context calls:
       `const [chatOpen, setChatOpen] = useState(true)`

    4. In the header section, inside the marginLeft:'auto' flex div (currently contains only the SSE status dot, lines ~102-119), add a collapse toggle button BEFORE the SSE dot:
       A button element with:
       - onClick: () => setChatOpen(prev => !prev)
       - title: chatOpen ? 'Collapse chat' : 'Expand chat'
       - style: background:'none', border:'none', cursor:'pointer', color:'#8b949e', fontSize:16, padding:'0 4px', lineHeight:1
       - Text content: chatOpen ? '›' : '‹'  (Unicode right/left chevron — collapse right, expand left)

    5. Replace the AI chat placeholder aside block (the block containing PlaceholderPanel label="AI CHAT") with:
       ```
       <aside
         style={{
           gridArea: 'chat',
           backgroundColor: '#1a1a2e',
           borderLeft: '1px solid #30363d',
           overflow: 'hidden',
           width: chatOpen ? '320px' : '40px',
           transition: 'width 200ms ease',
         }}
       >
         <ChatPanel />
       </aside>
       ```
       Note: the collapse is achieved by controlling width on the aside (not grid-template-columns in layout.tsx). The grid template remains '280px 1fr 320px' permanently; the aside shrinks and ChatPanel's content overflows hidden. ChatPanel stays mounted (never unmounts) — history persists per CHAT-06.

    6. Remove the PlaceholderPanel function at the bottom of page.tsx if it is no longer referenced anywhere else in the file. Check first with grep before removing.
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ChatPanel" frontend/app/page.tsx` returns at least 2 matches (import + usage)
    - `grep "chatOpen" frontend/app/page.tsx` returns at least 3 matches (useState, onClick, width ternary)
    - `grep "PlaceholderPanel" frontend/app/page.tsx` returns 0 matches (removed)
    - `grep "'AI CHAT'" frontend/app/page.tsx` returns 0 matches (placeholder gone)
    - `cd frontend && npm run build` exits 0 and frontend/out/index.html exists
    - `grep "transition.*width" frontend/app/page.tsx` returns 1 match (CSS transition on aside)
  </acceptance_criteria>
  <done>page.tsx imports and renders ChatPanel in the chat grid area. A toggle button in the header controls chatOpen state. The aside animates width between 320px and 40px. PlaceholderPanel is removed. npm run build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Write Playwright E2E tests for CHAT-01 through CHAT-06</name>
  <read_first>
    test/specs/smoke.spec.ts
    test/playwright.config.ts
  </read_first>
  <files>test/specs/chat.spec.ts</files>
  <action>
    Create test/specs/chat.spec.ts. Tests run against http://localhost:8001 with LLM_MOCK=true. Follow the exact import and constant pattern from smoke.spec.ts.

    File header:
    - `import { test, expect } from '@playwright/test';`
    - `const BASE = 'http://localhost:8001';`

    Six tests covering CHAT-01 through CHAT-06. Use section comment style matching smoke.spec.ts (e.g. `// ─── Chat Panel UI ───`).

    Test 1 — CHAT-01 'Chat panel renders input and message area':
    - Fixture: page
    - page.goto(BASE + '/'); await page.waitForTimeout(1500)
    - Assert: page.locator('input[placeholder="Ask FinAlly..."]').count() > 0
    - Assert: page.locator('text=Ask FinAlly anything about your portfolio').count() > 0 OR locator for the messages container

    Test 2 — CHAT-02 'Loading indicator appears while awaiting response':
    - Fixture: page
    - page.goto(BASE + '/'); await page.waitForTimeout(1500)
    - Type into input: await page.locator('input[placeholder="Ask FinAlly..."]').fill('What is my portfolio worth?')
    - Click send button: await page.locator('button:has-text("Send")').click()
    - Assert loading indicator appears: page.locator('text=FinAlly is thinking...').waitFor({ timeout: 3000 })

    Test 3 — CHAT-03 'Assistant message text is displayed after submit':
    - Fixture: page
    - page.goto(BASE + '/'); await page.waitForTimeout(1500)
    - Fill and send "What is my portfolio worth?"
    - await page.waitForTimeout(8000) (LLM mock still takes some time for backend processing)
    - Assert: page.locator('[data-role="assistant"]').count() > 0 OR check for text from mock response

    Test 4 — CHAT-04 'Trade chip appears for AI-executed trade':
    - Fixture: page
    - page.goto(BASE + '/'); await page.waitForTimeout(1500)
    - Send any message (e.g. "What is my portfolio worth?")
    - await page.waitForTimeout(8000)
    - Assert: await expect(page.locator('text=/BUY|SELL/')).toHaveCount(1)
    Note: Plan 04-01 Task 3 updated MOCK_RESPONSE to include trades:[{ticker:'AAPL',side:'buy',quantity:1}]. Every mock chat call executes a BUY of 1 AAPL (backend has sufficient cash: $10,000 starting balance). The chip renders "BUY 1 AAPL @ $price". This assertion is unconditional — no conditional fallback needed.

    Test 5 — CHAT-05 'Watchlist chip appears for AI watchlist change':
    - Fixture: page
    - page.goto(BASE + '/'); await page.waitForTimeout(1500)
    - Send any message (e.g. "What is my portfolio worth?")
    - await page.waitForTimeout(8000)
    - Assert: await expect(page.locator('text=/watchlist/i')).toHaveCount(1)
    Note: Plan 04-01 Task 3 updated MOCK_RESPONSE to include watchlist_changes:[{ticker:'COIN',action:'add'}]. Every mock call adds COIN to watchlist (backend returns 'added' or 'already_in_watchlist' — both produce a watchlist_results entry). The chip renders "+ COIN watchlist" or similar. This assertion is unconditional — no conditional fallback needed.

    Test 6 — CHAT-06 'Chat panel collapse toggle hides and restores panel':
    - Fixture: page
    - page.goto(BASE + '/'); await page.waitForTimeout(1500)
    - Assert input initially visible: page.locator('input[placeholder="Ask FinAlly..."]').isVisible()
    - Click the collapse button: page.locator('button[title="Collapse chat"]').click()
    - await page.waitForTimeout(300) (animation settles)
    - Assert input no longer visible (panel collapsed to 40px): `expect(await page.locator('input[placeholder="Ask FinAlly..."]').isVisible()).toBe(false)`
    - Click expand: page.locator('button[title="Expand chat"]').click()
    - await page.waitForTimeout(300)
    - Assert input visible again: page.locator('input[placeholder="Ask FinAlly..."]').isVisible()

    Important: All tests must work against a running container with LLM_MOCK=true. Tests that depend on AI response content (CHAT-03, CHAT-04, CHAT-05) use generous timeouts (8s) to accommodate backend processing. Use the request fixture for pre-trade setup (CHAT-04) to avoid flaky state.

    Do not use waitForResponse or network interception — keep tests aligned with smoke.spec.ts patterns (goto + waitForTimeout + locator assertions).
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/test && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at test/specs/chat.spec.ts
    - `grep -c "^test(" test/specs/chat.spec.ts` returns 6 (six test cases)
    - `grep "CHAT-0[1-6]" test/specs/chat.spec.ts | wc -l` returns at least 6 (each req referenced in comments)
    - `cd test && npx tsc --noEmit` exits 0 (TypeScript valid)
    - Each test references the BASE constant (no hardcoded URLs)
    - `grep "dangerouslySetInnerHTML\|innerHTML" test/specs/chat.spec.ts` returns 0 matches
    - `grep "localhost:8001" test/specs/chat.spec.ts` returns 0 matches (uses BASE constant instead)
  </acceptance_criteria>
  <done>test/specs/chat.spec.ts exists with 6 test cases covering CHAT-01 through CHAT-06, TypeScript compiles without errors, follows smoke.spec.ts patterns, uses BASE constant, generous timeouts for LLM mock responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → POST /api/chat | User-typed message crosses trust boundary to backend LLM |
| LLM response → chat UI | Backend returns message text that renders in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Injection (XSS) | ChatPanel message rendering | mitigate | Use {message.text} React JSX rendering (escapes HTML automatically); never use dangerouslySetInnerHTML |
| T-04-04 | Tampering | POST /api/chat input | accept | Backend validates and sanitizes all LLM inputs; frontend sends plain string; no client-side trust required |
| T-04-05 | Information Disclosure | Trade chip displays price | accept | Simulated portfolio, no real financial data; prices from same-origin API |
| T-04-06 | Denial of Service | isLoading guard | mitigate | Input and send button disabled during loading; handleSubmit guards on isLoading check at start |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all three tasks complete:
1. `cd /Users/richardleclezio/projects/finally/frontend && npm run build` exits 0 and frontend/out/index.html exists
2. `grep "ChatPanel" frontend/app/page.tsx | wc -l` outputs at least 2
3. `grep "refetchPortfolio" frontend/app/components/chat/ChatPanel.tsx | wc -l` outputs at least 1
4. `grep "executed_trades" frontend/app/components/chat/ChatPanel.tsx | wc -l` outputs at least 1
5. `grep -c "^test(" test/specs/chat.spec.ts` outputs 6
6. `cd /Users/richardleclezio/projects/finally/test && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- ChatPanel.tsx renders input, message list, spinner, trade chips, watchlist chips; TypeScript compiles
- page.tsx imports ChatPanel, removes PlaceholderPanel, adds collapse toggle with CSS width transition
- Collapse button in header toggles between 320px and 40px aside width; ChatPanel stays mounted
- After chat submit, portfolio positions and cash update without page reload (refetchPortfolio called)
- After watchlist chip, tickers array syncs without double-POST (refetchWatchlist called)
- 6 Playwright tests exist covering CHAT-01 through CHAT-06
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/04-ai-chat-panel/04-02-SUMMARY.md` when done
</output>
