# Plan 01-01 Summary — Next.js Scaffold, Config, Dark Theme & Grid Shell

**Status:** Complete
**Commit:** 276c1d6
**Completed:** 2026-06-28

## What Was Built

Replaced the empty `frontend` file with a fully configured Next.js 16.2.9 TypeScript project. The frontend compiles to a static export (`frontend/out/`) in production and proxies `/api/*` to the FastAPI backend in development.

## Files Created / Modified

| File | What Changed |
|------|-------------|
| `frontend/` | Replaced 0-byte file with full Next.js project directory |
| `frontend/next.config.ts` | Conditional `output: 'export'` (prod) + `/api/*` rewrites proxy (dev) |
| `frontend/app/globals.css` | Tailwind v4 `@theme` block with FinAlly brand colors, dark base styles, flash animations |
| `frontend/app/layout.tsx` | CSS Grid shell with named areas, `<html class="dark">`, PriceProvider wrapper |
| `frontend/app/page.tsx` | Dark placeholder panels for header/watch/chart/chat/port grid areas |
| `frontend/app/providers/PriceContext.tsx` | Singleton EventSource SSE hook with `price_update` named event listener |

## Key Decisions Made During Execution

1. **Next.js 16.2.9** installed (not 15 as planned — latest at execution time). Static export API unchanged.
2. **Tailwind CSS v4** confirmed — no `tailwind.config.ts`, CSS-based `@theme {}` configuration in `globals.css`.
3. **`output: 'export'` conditional on `NODE_ENV`** — rewrites and static export are mutually exclusive in Next.js; conditional prevents build errors.
4. **`images: { unoptimized: true }`** — required for static export, included upfront.
5. **Geist fonts dropped** — replaced with monospace system fonts matching terminal aesthetic.
6. **`change` field added to PriceUpdate** — backend `to_dict()` includes `change` (not just `change_percent`); type updated to match.

## Requirements Satisfied

| Req | Criterion | Verified |
|-----|-----------|---------|
| SETUP-02 | `frontend` empty file removed; Next.js directory created | ✓ |
| SETUP-03 | `output: 'export'` configured; Tailwind CSS dark theme applied | ✓ |
| SETUP-04 | `npm run build` exits 0; `frontend/out/index.html` exists | ✓ |
| VIS-01 | `#0d1117` / `#1a1a2e` backgrounds in `globals.css` and page | ✓ |
| VIS-02 | `#ecad0a`, `#209dd7`, `#753991` in `@theme` block | ✓ |

## Verification Results

```
npx tsc --noEmit     → 0 errors
npm run build        → exit 0, 4/4 static pages generated
frontend/out/index.html → exists, contains dark theme + FinAlly branding + CSS Grid
```

## Notes for Next Phase

- `frontend/out/` is gitignored — regenerated by `npm run build` (or Docker Stage 1)
- SSE hook in `PriceContext.tsx` is ready; Phase 2 components use `usePrice(ticker)` and `usePriceStatus()`
- Flash animation CSS classes `.flash-up` / `.flash-down` defined in `globals.css` — Phase 2 watchlist applies them
- Grid areas `watch`, `chart`, `chat`, `port` defined in `layout.tsx` — Phase 2/3/4 replace placeholder panels
