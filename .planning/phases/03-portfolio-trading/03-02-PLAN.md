---
phase: 03-portfolio-trading
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/app/components/portfolio/TradeBar.tsx
  - frontend/app/components/portfolio/PositionsTable.tsx
  - frontend/app/components/portfolio/PortfolioPanel.tsx
autonomous: false
requirements: [PORT-06, TRADE-01, TRADE-02, TRADE-03, TRADE-05]

must_haves:
  truths:
    - "User can enter a ticker and quantity and click Buy to purchase shares"
    - "After buying, cash decreases and position appears in positions table"
    - "User can sell shares; position updates or disappears when fully sold"
    - "Insufficient funds or shares shows error message (not a crash)"
    - "Positions table shows ticker, quantity (4dp trim), avg cost, current price, unrealized P&L, % change"
  artifacts:
    - path: "frontend/app/components/portfolio/TradeBar.tsx"
      provides: "Trade bar with ticker input, quantity input, Buy button, Sell button, error display"
    - path: "frontend/app/components/portfolio/PositionsTable.tsx"
      provides: "Positions table with 6 columns: ticker, qty, avg cost, price, P&L, % change"
  key_links:
    - from: "frontend/app/components/portfolio/TradeBar.tsx"
      to: "frontend/app/providers/PortfolioContext.tsx"
      via: "usePortfolio().executeTrade"
      pattern: "executeTrade"
    - from: "frontend/app/components/portfolio/PositionsTable.tsx"
      to: "frontend/app/providers/PortfolioContext.tsx"
      via: "usePortfolio().positions"
      pattern: "usePortfolio"
    - from: "frontend/app/components/portfolio/PositionsTable.tsx"
      to: "frontend/app/providers/PriceContext.tsx"
      via: "usePrice(ticker) for live price"
      pattern: "usePrice"
---

<objective>
Build the trade bar and positions table -- a complete trade execution vertical slice.

Purpose: After this plan, the user can execute buy and sell trades via the trade bar, see positions appear/update/disappear in the positions table with live prices and P&L, and see errors for invalid trades. This is the core trading interaction.

Output: TradeBar.tsx, PositionsTable.tsx, updated PortfolioPanel.tsx wiring.
</objective>

<execution_context>
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portfolio-trading/03-RESEARCH.md
@.planning/phases/03-portfolio-trading/03-PATTERNS.md
@.planning/phases/03-portfolio-trading/03-UI-SPEC.md
@.planning/phases/03-portfolio-trading/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/app/providers/PortfolioContext.tsx:
- Position type: { ticker: string, quantity: number, avg_cost: number, current_price: number, unrealized_pnl: number, pnl_percent: number }
- Trade type: { id: string, ticker: string, side: string, quantity: number, price: number, executed_at: string }
- usePortfolio() returns: { positions, cashBalance, totalValue, trades, history, executeTrade }
- executeTrade(ticker: string, quantity: number, side: 'buy' | 'sell'): Promise<string | null>

From frontend/app/providers/PriceContext.tsx:
- usePrice(ticker: string): PriceUpdate | undefined
- PriceUpdate: { ticker, price, previous_price, change, change_percent, direction, timestamp }

From frontend/app/components/watchlist/WatchlistPanel.tsx:
- Input style: height 28, background #0d1117, border 1px solid #30363d, color #e6edf3, fontSize 13, paddingLeft 8, no border-radius
- Button style: height 28, fontSize 11, fontWeight 600, color #ffffff, border none, borderRadius 0
- Error pattern: fontSize 11, color #ef4444, setTimeout 3000ms auto-dismiss
- Loading state: disabled inputs/buttons, opacity 0.6, cursor not-allowed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TradeBar and PositionsTable components</name>
  <files>
    frontend/app/components/portfolio/TradeBar.tsx
    frontend/app/components/portfolio/PositionsTable.tsx
  </files>
  <read_first>
    frontend/app/components/watchlist/WatchlistPanel.tsx
    frontend/app/components/watchlist/WatchlistRow.tsx
    frontend/app/providers/PortfolioContext.tsx
    frontend/app/providers/PriceContext.tsx
    .planning/phases/03-portfolio-trading/03-UI-SPEC.md
    .planning/phases/03-portfolio-trading/03-PATTERNS.md
  </read_first>
  <action>
    1. Create `frontend/app/components/portfolio/TradeBar.tsx`:
       - `'use client'` directive
       - Import useState from react, usePortfolio from providers/PortfolioContext
       - Local state: tickerInput (string), qtyInput (string), loading (boolean), error (string | null)
       - handleTrade async function taking side: 'buy' | 'sell':
         - Validate: ticker must be non-empty, qty must parse to a positive number. If invalid, set error and return.
         - setLoading(true), setError(null)
         - Call executeTrade(tickerInput.trim(), parseFloat(qtyInput), side)
         - If result is non-null (error string): setError(result), setTimeout(() => setError(null), 3000)
         - If result is null (success): clear tickerInput and qtyInput
         - finally: setLoading(false)
       - Render a 40px height bar with background #161b22, borderTop 1px solid #30363d, display flex, alignItems center, padding 0 8px, gap 4:
         - Ticker input: flex 1, minWidth 64, height 28, background #0d1117, border 1px solid #30363d, color #e6edf3, fontSize 13, paddingLeft 8, outline none, borderRadius 0. Placeholder "Ticker". onChange auto-uppercases. disabled={loading}.
         - Qty input: width 80, height 28, same input style. type="number", min="0", step="any". Placeholder "Qty". disabled={loading}.
         - Buy button: width 64, height 28, background #209dd7, color #ffffff, fontSize 11, fontWeight 600, border none, borderRadius 0. Text: loading ? "..." : "Buy". disabled={loading}. opacity: loading ? 0.6 : 1. cursor: loading ? 'not-allowed' : 'pointer'. onClick: () => handleTrade('buy').
         - Sell button: width 64, height 28, background #753991, same text/font style. Text: loading ? "..." : "Sell". Same disabled/opacity/cursor. onClick: () => handleTrade('sell').
       - Error display below the bar (outside the 40px flex): if error is non-null, render div with fontSize 11, color #ef4444, padding 2px 8px 0 8px, width 100%. Content: {error}.

    2. Create `frontend/app/components/portfolio/PositionsTable.tsx`:
       - `'use client'` directive
       - Import usePortfolio from providers/PortfolioContext, usePrice from providers/PriceContext
       - The component renders a flex column with height 100%:
         - Sticky header row: height 28, background #161b22, borderBottom 1px solid #30363d, display grid, gridTemplateColumns '52px 60px 68px 68px 72px 52px', alignItems center, padding 0 8px. Column labels: TICKER, QTY, AVG COST, PRICE, P&L, %. All label style: fontSize 11, fontWeight 600, color #8b949e, textAlign for TICKER left, rest right.
         - Scrollable body: flex 1, overflowY auto.
         - If positions.length === 0: centered "No positions" text, fontSize 11, color #30363d.
         - For each position: render a PositionRow sub-component (defined in same file).
       - PositionRow component receives position prop (Position type):
         - Call usePrice(position.ticker) to get live price
         - Compute live values: currentPrice = priceUpdate?.price ?? position.current_price. unrealizedPnl = (currentPrice - position.avg_cost) * position.quantity. pnlPercent = position.avg_cost > 0 ? ((currentPrice - position.avg_cost) / position.avg_cost) * 100 : 0.
         - Local hover state for background color
         - Render grid row: same gridTemplateColumns as header, height 36, padding 0 8px, borderBottom 1px solid #21262d, backgroundColor hover ? #161b22 : transparent, cursor default.
         - Columns (all fontSize 13, color #e6edf3):
           - Ticker: fontWeight 600, textAlign left
           - Qty: textAlign right, format parseFloat(position.quantity.toFixed(4)).toString()
           - Avg Cost: textAlign right, format $X.XX via toFixed(2)
           - Price: textAlign right, format $X.XX (currentPrice.toFixed(2))
           - P&L: textAlign right, color #22c55e if >= 0 else #ef4444, format: sign prefix + $X.XX
           - %: textAlign right, color #22c55e if >= 0 else #ef4444, format: +X.XX% / -X.XX%
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npx --yes tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - TradeBar.tsx exports a default function TradeBar with 'use client' directive
    - TradeBar has ticker input (auto-uppercase), qty input (type number), Buy button (#209dd7), Sell button (#753991)
    - TradeBar calls usePortfolio().executeTrade on button click
    - TradeBar shows error text in #ef4444 that auto-dismisses after 3s
    - TradeBar disables inputs and buttons during loading, shows "..." on buttons
    - PositionsTable.tsx exports a default function PositionsTable with 'use client' directive
    - PositionsTable has sticky 28px header row with 6 column labels in ALL CAPS
    - PositionRow calls usePrice(ticker) for live price recalculation
    - Quantity formatted with parseFloat(qty.toFixed(4)).toString()
    - P&L and % columns colored green (#22c55e) for positive, red (#ef4444) for negative
    - Empty state shows "No positions" centered
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>TradeBar and PositionsTable components built with full trade execution flow, live price recalculation, proper formatting, and error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TradeBar and PositionsTable into PortfolioPanel</name>
  <files>
    frontend/app/components/portfolio/PortfolioPanel.tsx
  </files>
  <read_first>
    frontend/app/components/portfolio/PortfolioPanel.tsx
    frontend/app/components/portfolio/TradeBar.tsx
    frontend/app/components/portfolio/PositionsTable.tsx
    .planning/phases/03-portfolio-trading/03-UI-SPEC.md
  </read_first>
  <action>
    Update PortfolioPanel.tsx to replace the Row B placeholder content with real components:
    - Import TradeBar and PositionsTable
    - Row B (bottom half of the two-row split, flex 1, display flex, flexDirection column) should contain:
      - TradeBar at the top (flexShrink 0 — it has its own fixed 40px height)
      - PositionsTable below (flex 1, minHeight 0 — scrollable within remaining space)
    - Row A (top half) keeps its placeholder for now (Heatmap + P&L chart come in Plan 03)
    - Ensure the Row B container has overflow hidden so PositionsTable handles its own scroll
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PortfolioPanel.tsx imports TradeBar and PositionsTable
    - Row B section contains TradeBar (fixed height) and PositionsTable (flex 1)
    - npm run build exits 0
    - No placeholder text remains in Row B
  </acceptance_criteria>
  <done>PortfolioPanel wires TradeBar and PositionsTable into the bottom half of the port area. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of trade execution flow</name>
  <what-built>Complete trade execution vertical slice: trade bar with Buy/Sell buttons, positions table showing live-updating positions with P&L.</what-built>
  <how-to-verify>
    1. Start backend and frontend dev servers
    2. Open http://localhost:3000
    3. In the trade bar (port area, below the placeholder charts section):
       a. Type "AAPL" in ticker field, "5" in qty field, click Buy
       b. Verify: position appears in positions table with AAPL, qty 5, avg cost, live price, P&L, %
       c. Verify: header cash balance decreased by ~$950 (5 shares x current AAPL price)
       d. Verify: header portfolio total value reflects the new position
    4. Try selling: type "AAPL", qty "5", click Sell
       a. Verify: AAPL position disappears from table (fully sold)
       b. Verify: cash balance returns to approximately $10,000
    5. Try an error: type "AAPL", qty "1000000", click Buy
       a. Verify: red error text appears below trade bar saying "Insufficient funds"
       b. Verify: error auto-dismisses after ~3 seconds
       c. Verify: no crash or blank screen
    6. Verify positions table columns: quantity shows up to 4dp (try buying 0.5 shares), P&L colors are green/red
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trade bar input -> /api/portfolio/trade | User-typed ticker and quantity sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Trade bar ticker input | mitigate | Auto-uppercase on input; backend normalizes and validates |
| T-03-05 | Tampering | Trade bar quantity input | mitigate | type="number" on input; backend rejects non-positive quantities |
| T-03-06 | Denial of Service | Double-submit trade | mitigate | Disable Buy/Sell buttons during loading state (loading=true) |
| T-03-07 | Information Disclosure | Backend error messages | accept | API detail field displayed verbatim via React JSX escaping; no sensitive data in error messages |
</threat_model>

<verification>
- npm run build exits 0
- Buy trade: position appears in table, cash decreases, header total updates
- Sell trade: position disappears, cash increases
- Error trade: red error text appears and auto-dismisses
- Positions table formats: qty 4dp trim, price $X.XX, P&L colored green/red
</verification>

<success_criteria>
- User can buy shares and see them appear in the positions table
- User can sell shares and see the position update or disappear
- Invalid trades show error messages that auto-dismiss
- Positions table has 6 columns with correct formatting and live prices
- Trade bar has Buy (blue) and Sell (purple) buttons that disable during loading
</success_criteria>

<output>
Create `.planning/phases/03-portfolio-trading/03-02-SUMMARY.md` when done
</output>
