---
phase: 03-portfolio-trading
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/app/components/portfolio/Heatmap.tsx
  - frontend/app/components/portfolio/PnLChart.tsx
  - frontend/app/components/portfolio/TradeHistory.tsx
  - frontend/app/components/portfolio/PortfolioPanel.tsx
autonomous: false
requirements: [PORT-04, PORT-05, TRADE-04]

must_haves:
  truths:
    - "Portfolio heatmap shows positions as colored rectangles sized by weight"
    - "Heatmap rectangles are green for profit, red for loss"
    - "P&L chart shows portfolio value over time as a line chart"
    - "Trade history shows recent trades with ticker, side, qty, price, time"
  artifacts:
    - path: "frontend/app/components/portfolio/Heatmap.tsx"
      provides: "recharts Treemap with custom content function for P&L coloring"
    - path: "frontend/app/components/portfolio/PnLChart.tsx"
      provides: "recharts LineChart showing portfolio value over time"
    - path: "frontend/app/components/portfolio/TradeHistory.tsx"
      provides: "Trade history list with side badges and formatted values"
  key_links:
    - from: "frontend/app/components/portfolio/Heatmap.tsx"
      to: "frontend/app/providers/PortfolioContext.tsx"
      via: "usePortfolio().positions"
      pattern: "usePortfolio"
    - from: "frontend/app/components/portfolio/PnLChart.tsx"
      to: "frontend/app/providers/PortfolioContext.tsx"
      via: "usePortfolio().history"
      pattern: "usePortfolio"
    - from: "frontend/app/components/portfolio/TradeHistory.tsx"
      to: "frontend/app/providers/PortfolioContext.tsx"
      via: "usePortfolio().trades"
      pattern: "usePortfolio"
---

<objective>
Build the heatmap, P&L chart, and trade history -- the visualization and activity vertical slice.

Purpose: After this plan, the user sees their positions visualized as a colored heatmap (green profit / red loss), a line chart of portfolio value over time, and a list of recent trades. Combined with Plan 02's trade bar and positions table, the complete portfolio section is fully functional.

Output: Heatmap.tsx, PnLChart.tsx, TradeHistory.tsx, updated PortfolioPanel.tsx with all components wired.
</objective>

<execution_context>
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portfolio-trading/03-RESEARCH.md
@.planning/phases/03-portfolio-trading/03-PATTERNS.md
@.planning/phases/03-portfolio-trading/03-UI-SPEC.md
@.planning/phases/03-portfolio-trading/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/app/providers/PortfolioContext.tsx:
- Position type: { ticker: string, quantity: number, avg_cost: number, current_price: number, unrealized_pnl: number, pnl_percent: number }
- Trade type: { id: string, ticker: string, side: string, quantity: number, price: number, executed_at: string }
- Snapshot type: { total_value: number, recorded_at: string }
- usePortfolio() returns: { positions, cashBalance, totalValue, trades, history, executeTrade }

From frontend/app/providers/PriceContext.tsx:
- usePrice(ticker: string): PriceUpdate | undefined
- PriceUpdate: { ticker, price, ... }

recharts exports needed:
- ResponsiveContainer, Treemap (from 'recharts') for heatmap
- ResponsiveContainer, LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip (from 'recharts') for P&L chart

Treemap data shape (RESEARCH.md):
- { name: string, value: number, unrealized_pnl: number, pnl_percent: number, [key: string]: unknown }
- content prop accepts a custom render function receiving TreemapNode props (x, y, width, height, name, value)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Heatmap, PnLChart, and TradeHistory components</name>
  <files>
    frontend/app/components/portfolio/Heatmap.tsx
    frontend/app/components/portfolio/PnLChart.tsx
    frontend/app/components/portfolio/TradeHistory.tsx
  </files>
  <read_first>
    frontend/app/providers/PortfolioContext.tsx
    frontend/app/providers/PriceContext.tsx
    frontend/app/components/chart/MainChart.tsx
    frontend/app/components/watchlist/WatchlistRow.tsx
    .planning/phases/03-portfolio-trading/03-RESEARCH.md
    .planning/phases/03-portfolio-trading/03-UI-SPEC.md
    .planning/phases/03-portfolio-trading/03-PATTERNS.md
  </read_first>
  <action>
    1. Create `frontend/app/components/portfolio/Heatmap.tsx`:
       - `'use client'` directive
       - Import useMemo from react, usePortfolio from providers/PortfolioContext, usePrice from providers/PriceContext, ResponsiveContainer and Treemap from recharts
       - Define HeatmapData type: { name: string, value: number, unrealized_pnl: number, pnl_percent: number, [key: string]: unknown }
       - Define a HeatmapCell function that renders custom treemap rectangles. It receives props and extracts x, y, width, height, name from props. Extract unrealized_pnl and pnl_percent by casting props to Record<string, unknown>. Fill color: pnl > 0 => 'rgba(34, 197, 94, 0.6)', pnl < 0 => 'rgba(239, 68, 68, 0.6)', breakeven => '#30363d'. Render an SVG g element with rect (fill, stroke #0d1117) and, if width > 30 and height > 24, two text elements centered: ticker name (11px, weight 600, fill #e6edf3) and pnl percent formatted as +X.XX% (11px, weight 600, fill #e6edf3).
       - Main Heatmap component:
         - Call usePortfolio() to get positions
         - For each position, call usePrice(position.ticker) to get live price. To do this without violating hook rules, create a HeatmapWrapper component that gets positions, then a HeatmapInner component that receives positions and uses a PositionPricer sub-component pattern. Actually, simpler approach: since positions are an array, use useMemo to transform positions into heatmap data using usePortfolio().positions and the prices map from useContext(PriceContext) directly (import PriceContext and useContext).
         - Compute heatmap data via useMemo: for each position, get currentPrice from prices[pos.ticker]?.price || pos.current_price. Compute positionValue = pos.quantity * currentPrice. Compute unrealizedPnl = (currentPrice - pos.avg_cost) * pos.quantity. Compute pnlPercent = pos.avg_cost > 0 ? ((currentPrice - pos.avg_cost) / pos.avg_cost) * 100 : 0. Return { name: pos.ticker, value: Math.abs(positionValue), unrealized_pnl: unrealizedPnl, pnl_percent: pnlPercent }.
         - Empty state: if positions.length === 0, render centered "No open positions" text (11px, color #30363d).
         - Otherwise render ResponsiveContainer width 100% height 100% wrapping Treemap with data={heatmapData}, dataKey="value", content={<HeatmapCell />}, isAnimationActive={false}, stroke="#0d1117".
         - Outer div: width 100%, height 100%, background #0d1117.

    2. Create `frontend/app/components/portfolio/PnLChart.tsx`:
       - `'use client'` directive
       - Import usePortfolio from providers/PortfolioContext
       - Import ResponsiveContainer, LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip from recharts
       - Helper: formatTime(iso: string): string => new Date(iso).toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit', hour12: false })
       - Helper: formatDollarAxis(value: number): string => '$' + value.toLocaleString()
       - Component reads history from usePortfolio()
       - Empty state: if history.length === 0, render centered "No history yet" (11px, color #30363d)
       - Otherwise: ResponsiveContainer width 100% height 100%, containing LineChart data={history}:
         - CartesianGrid horizontal vertical={false} stroke="#21262d" strokeDasharray="3 3"
         - XAxis dataKey="recorded_at" tickFormatter={formatTime} tick={{ fill: '#8b949e', fontSize: 11, fontWeight: 600 }} axisLine={false} tickLine={false}
         - YAxis tickFormatter={formatDollarAxis} tick={{ fill: '#8b949e', fontSize: 11, fontWeight: 600 }} orientation="right" axisLine={false} tickLine={false} width={60}
         - Tooltip contentStyle={{ backgroundColor: '#161b22', border: '1px solid #30363d', color: '#e6edf3', fontSize: 11 }}, formatter as a function returning [$value.toFixed(2), 'Value'], labelFormatter={formatTime}
         - Line type="monotone" dataKey="total_value" stroke="#209dd7" strokeWidth={1.5} dot={false} isAnimationActive={false}
       - Outer div: width 100%, height 100%, background #0d1117.

    3. Create `frontend/app/components/portfolio/TradeHistory.tsx`:
       - `'use client'` directive
       - Import useState from react, usePortfolio from providers/PortfolioContext
       - Component reads trades from usePortfolio()
       - Helper: formatTime(iso: string) => new Date(iso).toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false })
       - Helper: formatQuantity(qty: number) => parseFloat(qty.toFixed(4)).toString()
       - Render a flex column, height 100%:
         - 32px section header: "HISTORY" label, standard label style (11px/600/#8b949e/0.15em letterSpacing), padding 0 8px, borderBottom 1px solid #30363d, flexShrink 0
         - Scrollable body: flex 1, overflowY auto
         - Empty state: "No trades yet" centered (11px, color #30363d)
         - For each trade: a 32px flex row with padding 0 8px, borderBottom 1px solid #21262d, alignItems center, gap 8. Hover state with background #161b22.
           - Side badge: "BUY" or "SELL" text, width 32, fontSize 11, fontWeight 600, color #22c55e for buy, #ef4444 for sell
           - Ticker: fontSize 13, fontWeight 600, color #e6edf3, width 48
           - Qty: fontSize 11, fontWeight 600, color #8b949e, formatted with formatQuantity
           - Price: fontSize 11, fontWeight 600, color #8b949e, formatted as $X.XX
           - Time: fontSize 11, color #30363d, marginLeft auto, formatted with formatTime
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npx --yes tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Heatmap.tsx exports default Heatmap with 'use client' directive
    - Heatmap uses recharts Treemap with custom HeatmapCell content function
    - HeatmapCell colors rectangles green (rgba(34,197,94,0.6)) for profit, red (rgba(239,68,68,0.6)) for loss
    - HeatmapCell shows ticker name and P&L percent as centered text
    - Heatmap shows "No open positions" when positions array is empty
    - PnLChart.tsx exports default PnLChart with 'use client' directive
    - PnLChart uses recharts LineChart with blue line (#209dd7), right-aligned Y axis, time-formatted X axis
    - PnLChart shows "No history yet" when history array is empty
    - TradeHistory.tsx exports default TradeHistory with 'use client' directive
    - TradeHistory renders trades with BUY/SELL badges in green/red, formatted qty/price/time
    - TradeHistory has 32px "HISTORY" header with standard label style
    - TradeHistory shows "No trades yet" when trades array is empty
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Heatmap, PnLChart, and TradeHistory components built with recharts visualizations, proper formatting, and empty states.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Heatmap, PnLChart, TradeHistory into PortfolioPanel</name>
  <files>
    frontend/app/components/portfolio/PortfolioPanel.tsx
  </files>
  <read_first>
    frontend/app/components/portfolio/PortfolioPanel.tsx
    frontend/app/components/portfolio/Heatmap.tsx
    frontend/app/components/portfolio/PnLChart.tsx
    frontend/app/components/portfolio/TradeHistory.tsx
    .planning/phases/03-portfolio-trading/03-UI-SPEC.md
  </read_first>
  <action>
    Update PortfolioPanel.tsx to wire all components into the two-row layout:
    - Import Heatmap, PnLChart, TradeHistory (in addition to TradeBar and PositionsTable from Plan 02)
    - Row A (top half, flex 1, display flex, minHeight 0):
      - Left half (flex 1, minWidth 0): wrap in a div with width 100%, height 100%. Inside: a small section label area if desired, then the Heatmap component. The Heatmap needs a parent with explicit dimensions — use flex 1 with minHeight 0.
      - Right half (flex 1, minWidth 0): same wrapper, contains PnLChart
      - Separator between halves: 1px solid #30363d vertical border (use borderRight on left half)
    - Row B (bottom half, flex 1, display flex, flexDirection column, minHeight 0):
      - TradeBar (flexShrink 0 — fixed 40px)
      - PositionsTable (flex 1, minHeight 0)
      - TradeHistory (flex 1, minHeight 0, borderTop 1px solid #30363d)
    - Divider between Row A and Row B: borderTop or borderBottom 1px solid #30363d
    - Remove all placeholder text — every sub-section now has a real component

    Note: If Plan 02 has already been executed and wired TradeBar + PositionsTable into Row B, this task only needs to add Heatmap and PnLChart to Row A and add TradeHistory to Row B. If Plan 02 has NOT been executed yet (parallel execution), this task wires everything. Read PortfolioPanel.tsx first to see current state.
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PortfolioPanel.tsx imports Heatmap, PnLChart, TradeHistory
    - Row A contains Heatmap (left) and PnLChart (right) side by side with 1px border between
    - Row B contains TradeBar, PositionsTable, and TradeHistory
    - No placeholder text remains in the PortfolioPanel
    - npm run build exits 0
  </acceptance_criteria>
  <done>All portfolio components wired into PortfolioPanel. The port grid area is fully functional with heatmap, P&L chart, trade bar, positions table, and trade history.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of heatmap, P&L chart, and trade history</name>
  <what-built>Complete portfolio visualization: heatmap showing positions as colored rectangles, P&L line chart, and trade history list.</what-built>
  <how-to-verify>
    1. Start backend and frontend dev servers
    2. Open http://localhost:3000
    3. First, execute a trade to create a position: type "AAPL" / qty "10" / click Buy
    4. Verify heatmap:
       a. An AAPL rectangle appears in the heatmap
       b. Rectangle shows ticker "AAPL" and P&L percentage
       c. Rectangle is green or red depending on P&L direction
       d. Buy "TSLA" (5 shares) — verify a second rectangle appears, sized proportionally
    5. Verify P&L chart:
       a. A blue line appears showing portfolio value over time
       b. Wait ~10 seconds for backend to record more snapshots — line should have multiple points
       c. After another trade, the chart should update with the new value
    6. Verify trade history:
       a. "HISTORY" section label visible
       b. BUY AAPL and BUY TSLA trades appear with green "BUY" badges
       c. Each trade shows ticker, quantity, price, and time
    7. Sell all AAPL shares — verify:
       a. AAPL rectangle disappears from heatmap
       b. SELL trade appears in history with red "SELL" badge
       c. Positions table no longer shows AAPL
    8. Verify empty states: if you sell all positions, heatmap shows "No open positions"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/portfolio/history -> PnLChart | Backend snapshot data rendered as chart |
| /api/trades -> TradeHistory | Trade records displayed as list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | Trade history display | accept | Single-user app; all trades are the user's own |
| T-03-09 | Tampering | Heatmap data | accept | Data derived from cached positions + SSE prices, both from trusted same-origin API |
| T-03-10 | Spoofing | recharts tooltip | accept | Tooltip renders data already in the DOM; no external input |
</threat_model>

<verification>
- npm run build exits 0
- Heatmap shows colored rectangles for positions (green profit / red loss)
- P&L chart shows blue line with portfolio value over time
- Trade history shows recent trades with colored side badges
- Empty states render correctly when no positions/history/trades exist
</verification>

<success_criteria>
- Heatmap renders positions as rectangles sized by weight, colored by P&L
- P&L chart shows portfolio value line chart with proper axes and tooltip
- Trade history displays recent trades with BUY/SELL badges, formatted values
- All components update after trade execution
- Port area is fully populated with no placeholders remaining
</success_criteria>

<output>
Create `.planning/phases/03-portfolio-trading/03-03-SUMMARY.md` when done
</output>
