---
phase: 03-portfolio-trading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/app/providers/PortfolioContext.tsx
  - frontend/app/components/portfolio/PortfolioPanel.tsx
  - frontend/app/page.tsx
  - frontend/app/layout.tsx
autonomous: false
requirements: [PORT-01, PORT-02, PORT-03]

must_haves:
  truths:
    - "Header shows live total portfolio value that updates as SSE prices stream"
    - "Header shows current cash balance ($10,000.00 on fresh start)"
    - "Header shows SSE connection status dot (green when connected)"
    - "Port grid area shows a PortfolioPanel container instead of placeholder"
  artifacts:
    - path: "frontend/app/providers/PortfolioContext.tsx"
      provides: "Portfolio state provider with positions, cash, totalValue, trades, history, executeTrade"
      exports: ["PortfolioProvider", "usePortfolio"]
    - path: "frontend/app/components/portfolio/PortfolioPanel.tsx"
      provides: "Port grid area container with two-row layout shell"
    - path: "frontend/app/page.tsx"
      provides: "Header with portfolio value, cash, status dot; port area with PortfolioPanel"
  key_links:
    - from: "frontend/app/providers/PortfolioContext.tsx"
      to: "/api/portfolio"
      via: "fetch on mount"
      pattern: "fetch.*api/portfolio"
    - from: "frontend/app/providers/PortfolioContext.tsx"
      to: "frontend/app/providers/PriceContext.tsx"
      via: "useContext(PriceContext) for SSE prices"
      pattern: "useContext.*PriceContext"
    - from: "frontend/app/page.tsx"
      to: "frontend/app/providers/PortfolioContext.tsx"
      via: "usePortfolio() hook"
      pattern: "usePortfolio"

user_setup:
  - service: npm-packages
    why: "recharts and react-is must be installed"
    dashboard_config:
      - task: "Verify recharts and react-is packages on npmjs.com before install"
        location: "https://www.npmjs.com/package/recharts and https://www.npmjs.com/package/react-is"
---

<objective>
Install recharts, create PortfolioContext provider, and wire live portfolio values into the header and port grid area.

Purpose: After this plan, the user sees live-updating portfolio total value and cash balance in the header, a connection status dot replacing the "LIVE" text, and a real PortfolioPanel container in the port grid area. This is the foundation all other portfolio components build on.

Output: PortfolioContext.tsx, PortfolioPanel.tsx shell, updated page.tsx with header additions, updated layout.tsx with PortfolioProvider.
</objective>

<execution_context>
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portfolio-trading/03-RESEARCH.md
@.planning/phases/03-portfolio-trading/03-PATTERNS.md
@.planning/phases/03-portfolio-trading/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/app/providers/PriceContext.tsx:
- PriceUpdate type: { ticker, price, previous_price, change, change_percent, direction, timestamp }
- PriceContextValue: { prices: Record<string, PriceUpdate>, status: 'connecting' | 'connected' | 'reconnecting' | 'disconnected' }
- exports: PriceProvider, PriceContext, usePrice(ticker), usePriceStatus()

From frontend/app/providers/WatchlistContext.tsx:
- WatchlistContextValue type with tickers, selectedTicker, priceHistory, firstPrice, addTicker, removeTicker
- exports: WatchlistProvider, WatchlistContext, useWatchlist()

Backend API response — GET /api/portfolio:
{ cash_balance: number, total_value: number, unrealized_pnl: number, positions: [{ ticker, quantity, avg_cost, current_price, unrealized_pnl, pnl_percent }] }

Backend API response — GET /api/trades:
[{ id, ticker, side, quantity, price, executed_at }]

Backend API response — GET /api/portfolio/history:
[{ total_value, recorded_at }]

Backend API response — POST /api/portfolio/trade:
Success: { success: true, trade: { id, ticker, side, quantity, price, executed_at }, new_cash_balance }
Error (400): { detail: "Insufficient funds..." } or { detail: "Insufficient shares..." }
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify recharts and react-is package legitimacy</name>
  <what-built>Package legitimacy check before npm install. Both packages are tagged [ASSUMED] in RESEARCH.md because slopcheck was unavailable.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/recharts — confirm it is the official recharts charting library (github.com/recharts/recharts), ~8 years old, high download count
    2. Visit https://www.npmjs.com/package/react-is — confirm it is the official React team package (github.com/facebook/react), very high download count
    3. Both should show legitimate maintainers and recent publish activity
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with npm install, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install recharts + create PortfolioContext + PortfolioPanel shell</name>
  <files>
    frontend/package.json
    frontend/package-lock.json
    frontend/app/providers/PortfolioContext.tsx
    frontend/app/components/portfolio/PortfolioPanel.tsx
  </files>
  <read_first>
    frontend/app/providers/WatchlistContext.tsx
    frontend/app/providers/PriceContext.tsx
    frontend/app/components/watchlist/WatchlistPanel.tsx
    frontend/AGENTS.md
    .planning/phases/03-portfolio-trading/03-RESEARCH.md
    .planning/phases/03-portfolio-trading/03-PATTERNS.md
    .planning/phases/03-portfolio-trading/03-UI-SPEC.md
  </read_first>
  <action>
    1. Run `cd frontend && npm install recharts react-is` to add charting dependencies.

    2. Create `frontend/app/providers/PortfolioContext.tsx` following the WatchlistContext pattern exactly:
       - `'use client'` directive at top
       - Define types: Position (ticker, quantity, avg_cost, current_price, unrealized_pnl, pnl_percent), Trade (id, ticker, side, quantity, price, executed_at), Snapshot (total_value, recorded_at)
       - Define PortfolioContextValue type with: positions (Position[]), cashBalance (number), totalValue (number), trades (Trade[]), history (Snapshot[]), executeTrade ((ticker: string, quantity: number, side: 'buy' | 'sell') => Promise<string | null>)
       - Export PortfolioContext with createContext and sensible defaults
       - Export PortfolioProvider function component:
         - useContext(PriceContext) to get `prices` for live recalculation
         - useState for positions, cashBalance, trades, history
         - Mount effect: fetch GET /api/portfolio (extract positions + cash_balance), GET /api/trades, GET /api/portfolio/history. Silent catch on errors.
         - Live totalValue recalculation via useMemo: iterate positions, for each look up prices[pos.ticker]?.price (fall back to pos.current_price if no SSE data yet), multiply by pos.quantity, sum all + cashBalance. Dependencies: [positions, cashBalance, prices]
         - executeTrade function: POST /api/portfolio/trade with JSON body {ticker: ticker.toUpperCase(), quantity, side}. On success (resp.ok): re-fetch all three endpoints (portfolio, trades, history), return null. On error: parse response JSON, return detail string. If JSON parse fails, return "Trade failed. Try again."
         - Helper function refetchAll that fetches GET /api/portfolio, GET /api/trades, GET /api/portfolio/history and updates all state. Use useCallback to stabilize reference.
       - Export usePortfolio() hook that returns useContext(PortfolioContext)

    3. Create `frontend/app/components/portfolio/PortfolioPanel.tsx`:
       - `'use client'` directive
       - Full-height flex column container (same as WatchlistPanel: height 100%, display flex, flexDirection column)
       - 32px panel header with "PORTFOLIO" label using standard label style: fontSize 11, fontWeight 600, color #8b949e, letterSpacing 0.15em, padding 0 8px, borderBottom 1px solid #30363d
       - Content area with flex 1, overflow hidden. Inside: a two-row flex layout. Row A (flex 1, display flex, gap 0) will hold Heatmap + PnLChart side by side. Row B (flex 1, display flex, flexDirection column) will hold TradeBar + PositionsTable + TradeHistory.
       - For now, Row A and Row B contain empty placeholder divs with centered text: "Heatmap / P&L Chart" and "Trade Bar / Positions" in fontSize 11, color #30363d. These will be replaced in Plans 02 and 03.
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && grep -q '"recharts"' package.json && node -e "require('recharts')" 2>/dev/null; echo "recharts installed: $?"</automated>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npx --yes tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "recharts" and "react-is" in dependencies
    - frontend/app/providers/PortfolioContext.tsx exports PortfolioProvider, PortfolioContext, usePortfolio
    - PortfolioContext.tsx defines Position, Trade, Snapshot types and PortfolioContextValue type
    - PortfolioProvider calls fetch('/api/portfolio') on mount
    - totalValue is computed via useMemo from positions + prices + cashBalance (NOT by polling GET /api/portfolio)
    - executeTrade returns Promise<string | null> (error message or null)
    - frontend/app/components/portfolio/PortfolioPanel.tsx exports default function PortfolioPanel
    - PortfolioPanel has 32px header with "PORTFOLIO" label and two-row flex content area
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>PortfolioContext provider created with fetch-on-mount, SSE-driven totalValue recalculation, and executeTrade function. PortfolioPanel shell renders with placeholder sub-sections. recharts installed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PortfolioProvider into layout + header live values + status dot in page.tsx</name>
  <files>
    frontend/app/layout.tsx
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/app/layout.tsx
    frontend/app/page.tsx
    frontend/app/providers/PortfolioContext.tsx
    frontend/app/providers/PriceContext.tsx
    .planning/phases/03-portfolio-trading/03-UI-SPEC.md
  </read_first>
  <action>
    1. Update layout.tsx: Import PortfolioProvider from ./providers/PortfolioContext. Nest it inside WatchlistProvider wrapping the grid div:
       PriceProvider > WatchlistProvider > PortfolioProvider > div(grid) > {children}

    2. Update page.tsx:
       - Add `'use client'` directive at top (needed for hooks in header)
       - Import usePortfolio from providers/PortfolioContext
       - Import usePriceStatus from providers/PriceContext
       - Import PortfolioPanel from components/portfolio/PortfolioPanel

       Header modifications (between "AI Trading Workstation" span and the marginLeft auto section):
       - Add a PORTFOLIO label + value group: a div with display flex, flexDirection column, gap 0. Inside: span "PORTFOLIO" with fontSize 11, fontWeight 600, color #8b949e, letterSpacing 0.15em. Below it: span showing totalValue formatted as USD with toLocaleString('en-US', {style: 'currency', currency: 'USD', minimumFractionDigits: 2, maximumFractionDigits: 2}), fontSize 13, fontWeight 600, color #e6edf3.
       - Add a CASH label + value group: same layout. Label "CASH", value showing cashBalance formatted the same way, fontSize 13, fontWeight 400, color #e6edf3.

       Replace the "LIVE" span (marginLeft auto section) with a status dot:
       - A div with marginLeft auto, display flex, alignItems center, gap 8
       - Inside: a div (the dot) with width 8, height 8, borderRadius '50%', backgroundColor mapped from usePriceStatus(): 'connected' => '#22c55e', 'reconnecting' => '#ecad0a', 'disconnected' => '#ef4444', 'connecting' => '#ecad0a'. Add title attribute for tooltip: 'Connected', 'Reconnecting...', 'Disconnected', or 'Connecting...'

       Replace the port section placeholder:
       - Remove PlaceholderPanel usage in the port section
       - Render PortfolioPanel component inside the port section (keep the section wrapper with gridArea port, backgroundColor #1a1a2e, borders)
       - Keep PlaceholderPanel function for the chat section (still Phase 4 placeholder)
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports PortfolioProvider and nests it inside WatchlistProvider
    - page.tsx has 'use client' directive
    - page.tsx imports and calls usePortfolio() and usePriceStatus()
    - Header contains a span with "PORTFOLIO" label (11px/600/#8b949e) and totalValue formatted as $X,XXX.XX
    - Header contains a span with "CASH" label (11px/600/#8b949e) and cashBalance formatted as $X,XXX.XX
    - Header contains an 8px status dot with background color derived from usePriceStatus()
    - The "LIVE" text is replaced by the status dot
    - Port section renders PortfolioPanel instead of PlaceholderPanel
    - npm run build exits 0
  </acceptance_criteria>
  <done>Header shows live portfolio value, cash balance, and SSE status dot. Port area renders PortfolioPanel shell. Layout has PortfolioProvider in the provider tree. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of header and portfolio shell</name>
  <what-built>Header with live portfolio value ($10,000.00 initial), cash balance ($10,000.00), green status dot. Port area shows PortfolioPanel with placeholder sub-sections.</what-built>
  <how-to-verify>
    1. Start the backend: cd backend && uv run uvicorn app.main:app --host 127.0.0.1 --port 8000
    2. Start the frontend: cd frontend && npm run dev
    3. Open http://localhost:3000 in browser
    4. Verify header shows: "FinAlly" | "AI Trading Workstation" | "PORTFOLIO $10,000.00" | "CASH $10,000.00" | green dot (right side)
    5. Verify the portfolio total value updates within 1-2 seconds as SSE prices stream (value should stay $10,000.00 if no positions exist yet, but the status dot should be green)
    6. Verify the port area (bottom-left) shows "PORTFOLIO" header and placeholder text for sub-sections
    7. Verify the watchlist and chart still work correctly (no regression)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /api/portfolio | Fetches portfolio data; no auth, single-user |
| Browser -> /api/portfolio/trade | Sends trade requests; backend validates all inputs |
| SSE -> Browser | Price data flows server-to-client; parsed by PriceContext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Trade bar ticker/qty input | mitigate | Backend validates: uppercase ticker, positive quantity, valid side. Frontend disables buttons during request to prevent double-submit |
| T-03-02 | Information Disclosure | Portfolio values in header | accept | Single-user app, no PII, simulated money |
| T-03-03 | Tampering | API error messages displayed | mitigate | React auto-escapes JSX strings; display API detail field via {value} not dangerouslySetInnerHTML |
| T-03-SC | Tampering | npm install recharts react-is | mitigate | Blocking human checkpoint (Task 0) to verify package legitimacy on npmjs.com before install |
</threat_model>

<verification>
- npm run build exits 0 in frontend/
- Header shows "PORTFOLIO" label with dollar value and "CASH" label with dollar value
- Status dot is visible (8px circle) with correct color for connection state
- PortfolioPanel renders in port grid area with PORTFOLIO header
- No TypeScript errors
</verification>

<success_criteria>
- User sees live portfolio value and cash in header on page load
- Status dot reflects SSE connection state (green when connected)
- Port area contains PortfolioPanel shell (not placeholder)
- PortfolioContext provides positions, cashBalance, totalValue, trades, history, executeTrade to descendants
- npm run build succeeds
</success_criteria>

<output>
Create `.planning/phases/03-portfolio-trading/03-01-SUMMARY.md` when done
</output>
