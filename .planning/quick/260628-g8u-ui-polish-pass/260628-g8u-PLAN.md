---
phase: quick
plan: 260628-g8u
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/app/components/chart/MainChart.tsx
  - frontend/app/components/watchlist/WatchlistRow.tsx
  - frontend/app/globals.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "PortfolioPanel (heatmap, P&L chart, positions table, trade bar, trade history) renders in a wide bottom section spanning at least 2/3 of viewport width"
    - "WatchlistPanel and PortfolioPanel are NOT stacked in the same 280px column"
    - "Main chart area has a visually polished area series with subtle gradient fill and adequate chart header"
    - "Header portfolio value reads at a larger, bolder size than supporting labels"
    - "Watchlist rows show legible price/change data with clear selected-state highlight"
    - "npm run build succeeds with no TypeScript errors"
---

<objective>
Restructure the CSS Grid to give PortfolioPanel its own wide bottom section, and apply targeted visual polish to the header, watchlist rows, and main chart to achieve a Bloomberg-meets-modern-dark-SaaS aesthetic.

Purpose: The current layout squeezes PortfolioPanel into the same 280px left column as the watchlist, making heatmap/charts/tables unreadable. This is the primary UX blocker before the project can be demoed.

Output:
- Revised 4-column grid in layout.tsx + page.tsx that gives portfolio a full-width bottom row (cols 1–3, excluding chat)
- Visual polish in header (larger portfolio value), watchlist rows (improved selected/hover states), and main chart (thicker line, better gradient, polished header bar)
</objective>

<execution_context>
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardleclezio/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/richardleclezio/projects/finally/frontend/app/layout.tsx
@/Users/richardleclezio/projects/finally/frontend/app/page.tsx
@/Users/richardleclezio/projects/finally/frontend/app/globals.css
@/Users/richardleclezio/projects/finally/frontend/app/components/chart/MainChart.tsx
@/Users/richardleclezio/projects/finally/frontend/app/components/watchlist/WatchlistRow.tsx

<interfaces>
<!-- Color palette (from globals.css @theme) -->
bg-base:    #0d1117
bg-panel:   #1a1a2e
bg-surface: #161b22
accent-yellow: #ecad0a
accent-blue:   #209dd7
accent-purple: #753991
text-primary:  #e6edf3
text-muted:    #8b949e
border:        #30363d

<!-- Current grid (layout.tsx) -->
gridTemplateAreas:
  "header header header"
  "watch  chart  chat"
  "port   chart  chat"
gridTemplateColumns: '280px 1fr 320px'
gridTemplateRows: '48px 1fr 1fr'

<!-- Grid area names in use (page.tsx) -->
header → <header>
watch  → <aside> (WatchlistPanel)
chart  → <main>  (MainChart) — spans rows 2+3 in current layout
chat   → <aside> (ChatPanel) — spans rows 2+3
port   → <section> (PortfolioPanel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure CSS Grid — portfolio gets wide bottom row</name>
  <files>frontend/app/layout.tsx, frontend/app/page.tsx</files>
  <action>
Redesign the grid in layout.tsx and update page.tsx element grid areas accordingly.

NEW GRID DESIGN (4 columns, 3 rows):

gridTemplateAreas:
  "header header header header"
  "watch  chart  chart  chat"
  "port   port   port   chat"
gridTemplateColumns: '220px 1fr 1fr 320px'
gridTemplateRows: '48px 1fr 40vh'
height: '100vh'

This gives:
- Row 1: header spans all 4 cols (unchanged)
- Row 2: watchlist 220px | chart spans 2 center cols (1fr + 1fr) | chat 320px
- Row 3: portfolio spans cols 1–3 (220px + 1fr + 1fr) | chat continues down

The `40vh` for the portfolio row ensures it gets substantial height (~40% of viewport) while the chart gets the remaining middle section.

In layout.tsx: update the gridTemplateAreas string, gridTemplateColumns, and gridTemplateRows values as above.

In page.tsx: the grid area assignments for each element stay the same (header/watch/chart/port/chat) — no changes to gridArea values needed. The named areas handle the spanning automatically.

Also in page.tsx:
- Increase header portfolio value font size from 13px to 16px and fontWeight from 600 to 700
- Increase header cash balance font size from 13px to 15px
- Add a vertical separator (1px solid #30363d, height 28px) between the "FinAlly" wordmark group and the portfolio/cash stat group, using a plain div with borderLeft
- Bump the header height in layout.tsx from 48px (gridTemplateRows first value) to 52px for better breathing room, and update the header element's height/minHeight accordingly

The chat aside in page.tsx currently sets `width: chatOpen ? '320px' : '40px'` inline — leave that intact. When chat is collapsed the extra space will be taken by the chart/port cells naturally since the grid column is defined at the layout level. To handle collapse gracefully, change the chat column from a fixed `320px` to use CSS custom property or keep fixed — keeping `320px` is fine for now since the collapse animation already works via the aside's own width style.
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. In the running app, PortfolioPanel occupies a wide bottom strip (~40vh tall, spanning 3 of 4 columns) instead of being stacked below the watchlist in a 280px column.</done>
</task>

<task type="auto">
  <name>Task 2: Polish main chart and watchlist row visuals</name>
  <files>frontend/app/components/chart/MainChart.tsx, frontend/app/components/watchlist/WatchlistRow.tsx, frontend/app/globals.css</files>
  <action>
Apply targeted visual polish to the chart and watchlist rows. Do NOT change component logic, hooks, or effects — only style values.

MAIN CHART (MainChart.tsx):

In the chart initialization (Effect 1), update AreaSeries options:
- topColor: 'rgba(32, 157, 215, 0.28)'  (slightly more transparent top)
- bottomColor: 'rgba(32, 157, 215, 0.0)'
- lineColor: '#209dd7'
- lineWidth: 3  (up from 2 — more visible line)

Update chart options:
- layout.background color stays '#0d1117'
- grid.horzLines: change color from '#161b22' to '#1a1e24' (slightly more visible)
- Add fontFamily to layout: 'JetBrains Mono, Cascadia Code, Fira Code, Consolas, monospace' (matches body font)

Chart header bar (the 32px div above the canvas):
- Change height from 32 to 40
- Add backgroundColor: '#0f1318' (slightly distinct from chart canvas bg)
- Add a bottom gradient line effect: boxShadow: '0 1px 0 #30363d, 0 2px 0 rgba(32,157,215,0.15)' (subtle blue glow on border)
- Ticker symbol: increase fontSize to 15, fontWeight to 700, add letterSpacing: '0.04em'
- Live price value: increase fontSize to 15, fontWeight to 600
- Add a placeholder message when no ticker is selected: centered text "Select a ticker to view chart" in #8b949e at 12px

WATCHLIST ROWS (WatchlistRow.tsx):

Update the row container div:
- height: 38 (up from 36 — slightly more comfortable)
- Row background for isSelected: '#1a2030' (slight blue tint vs generic #161b22)
- Row background for hovered (not selected): '#161b22'
- borderBottom color: '#21262d' (unchanged, fine)
- Selected left border: keep '2px solid #209dd7'
- Add transition: 'background-color 120ms ease' to the row container

Update the ticker symbol span:
- fontSize: 12 (slightly smaller — tighter look with monospace)
- fontWeight: 700 (bolder)
- letterSpacing: '0.04em'

Update the price cell div:
- fontSize: 12
- fontWeight: 600

Update the change % span:
- fontSize: 10 (smaller, de-emphasized)
- Add a background pill for positive values: for changePercent >= 0, backgroundColor: 'rgba(34,197,94,0.12)', padding: '1px 4px', borderRadius: 2
- For negative values: backgroundColor: 'rgba(239,68,68,0.12)', padding: '1px 4px', borderRadius: 2
- Apply these background/padding/borderRadius styles unconditionally (null case: same styling with muted color)

Keep the sparkline and remove button columns exactly as they are.

GLOBALS.CSS:

Add a scrollbar styling block at the end of the file for the watchlist scroll container (WebKit only — does not affect layout):

```css
/* Thin scrollbars for panel overflow containers */
::-webkit-scrollbar {
  width: 4px;
  height: 4px;
}
::-webkit-scrollbar-track {
  background: transparent;
}
::-webkit-scrollbar-thumb {
  background: #30363d;
  border-radius: 2px;
}
::-webkit-scrollbar-thumb:hover {
  background: #484f58;
}
```

Do NOT use fenced code blocks in this action — the CSS above is for reference only; write it as plain CSS at the bottom of globals.css.
  </action>
  <verify>
    <automated>cd /Users/richardleclezio/projects/finally/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. Chart line is visibly thicker (lineWidth 3). Chart header shows ticker at larger size. Watchlist rows have pill-style change % badges. Scrollbars are slim. No TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure styling/layout changes — no new trust surfaces introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g8u-01 | Tampering | CSS changes | accept | No user input processed; pure layout/visual — no attack surface |
</threat_model>

<verification>
After both tasks complete, verify:
1. `npm run build` exits 0 with no TypeScript errors
2. In the running app (http://localhost:8001 or dev server), PortfolioPanel occupies a wide bottom strip — heatmap and P&L chart are side-by-side with sufficient width to render meaningfully
3. MainChart header shows ticker name at 15px bold
4. WatchlistRow change % values have colored pill backgrounds
5. No visual regressions in ChatPanel or other components
</verification>

<success_criteria>
- Grid layout gives PortfolioPanel ~40vh height spanning 3 of 4 columns
- Chart lineWidth is 3 (was 2)
- Watchlist rows have transition animations and pill-style change badges
- `npm run build` passes clean
- No new npm packages added
</success_criteria>

<output>
Create `.planning/quick/260628-g8u-ui-polish-pass/260628-g8u-SUMMARY.md` when done
</output>
